#include <stdio.h>
int main()
{
   int num;
   scanf("%d",&num);
   if (num>999 & num<100){
   printf ("the num is three digit");
   }
   else {
   printf("the num is not three digit ");
   }
   return 0;
   }
